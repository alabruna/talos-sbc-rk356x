---
kind: pkgfile.Build
spec:
  targets:
    - talos-sbc-rk356x-mainline
    - talos-kernel-rk356x-mainline
  extraBuildArgs:
    - PKGS_PREFIX
    - PKGS
  makefile:
    extraVariables:
      - name: PKGS_PREFIX
        defaultValue: ghcr.io/siderolabs
      - name: PKGS
        defaultValue: v1.8.0
---
kind: common.Build
spec:
    ignoredPaths:
      - go.work.sum
