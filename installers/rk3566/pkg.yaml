name: overlay-rk356x
variant: scratch
shell: /toolchain/bin/bash
dependencies:
  - stage: base
steps:
  - env:
      GOPATH: /go
    cachePaths:
      - /.cache/go-build
      - /go/pkg
    build:
      - |
        export PATH=${PATH}:${TOOLCHAIN}/go/bin

        cd /pkg/src
        CGO_ENABLED=0 go build -o ./rk356x .
    install:
      - |
        mkdir -p /rootfs/installers/

        cp -p /pkg/src/rk356x /rootfs/installers/rk356x
finalize:
  - from: /rootfs
    to: /rootfs
